#!/usr/bin/python

import os
import random
import sys

WALLPAPERS_DIR = "/home/habibi/.config/wallpapers"

os.system("set_wallpaper " + str(random.choice(os.listdir(WALLPAPERS_DIR))))

print(sys.argv)
